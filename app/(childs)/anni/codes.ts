export const STEP_ONE = `# bun add anni`

export const STEP_TWO = `import { Toaster } from 'anni'

export default function App() {
  return (
    <body>
      <Toaster />
    </body>
  )
}`

export const STEP_THREE = `import { toast } from 'anni'

toast('Hello') // Run anywhere in your app`
